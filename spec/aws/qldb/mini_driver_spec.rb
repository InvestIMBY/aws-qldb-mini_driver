# frozen_string_literal: true

RSpec.describe Aws::Qldb::MiniDriver do
  it "has a version number" do
    expect(Aws::Qldb::MiniDriver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
